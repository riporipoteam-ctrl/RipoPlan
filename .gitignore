node_modules
.next
out
build
.env
.env.local
.env*.local
.DS_Store
*.pem
npm-debug.log*
.vercel
next-env.d.ts
.turbo

# Capacitor native projects (generated in CI). Our real native apps live in
# ios-native/ and android/ and ARE tracked — only ignore Capacitor's build dirs.
/ios/
.capacitor/

# TypeScript incremental build cache
*.tsbuildinfo
tsconfig.tsbuildinfo
